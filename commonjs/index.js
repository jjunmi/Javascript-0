const { perfectScore, sum, avg, subtract } = require('./math.js');

console.log('perfectScore:', perfectScore, 'sum:', sum(80, 10), 'avg:', avg(80, 90));
console.log('subtract:', subtract(20, 10));

/*
const math = require('./math.js');

console.log('perfectScore:', math.perfectScore, 'sum:', math.sum(80, 10), 'avg:', math.avg(80, 90));
console.log('subtract:', math.subtract(20, 10));
*/